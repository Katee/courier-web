= simple_form_for(@drop, :html => { :class => "form-horizontal" } ) do |f|
  .form-inputs
    = f.association :location

  .form-actions
    = f.button :submit, :class => "btn btn-primary"
    or
    = link_to "Cancel", :back
