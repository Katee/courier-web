%h1 Clients
%a{ :href => new_client_path }
  %i.icon-plus
  New Client

%table.table.table-striped
  %thead
    %tr
      %th
  %tbody
    - @clients.each do |client|
      %tr
        %td= link_to client.name, client

